version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"

# Ça suffit pour que Dependabot surveille tes actions GitHub dans .github/workflows/.